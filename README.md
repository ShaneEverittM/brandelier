# Brandelier
_"As opposed to a brandolier, which would be used for warfare"_

### To run the frontend locally:
> $ uv run flask run

To make the server automatically hot-load all changes made to local
files without requiring a process restart, add the `--debug` flag to that command.
